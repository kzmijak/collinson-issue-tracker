<!-- Copy to specs/NNN-<slug>/spec.md, delete this comment, fill in What I want,
     then run `pnpm enrich NNN`. Everything below the generated marker is written by the
     tool and must not be edited by hand.
     Why it is shaped this way: .claude/rules/specs.md -->

# NNN — <title>

Status: draft
Covers: work-order step N in PLAN.md

## What I want

### <YYYY-MM-DD> — <short label: what this entry decides>

<Prose. What the thing should do, the way you would explain it to a colleague. No tables, no
structure, no test cases — those are generated. Any number you already know belongs here; any
number you do not comes back as an open question rather than being guessed at.>

<!-- Every later change is a new dated heading appended below this one. Never edit an entry that
     is already here: it is the record of what was decided and when. Two entries may share a
     date — order comes from position in the file, not from the date. Give each a distinct
     label; two identical headings are an error. -->

<!-- enrich:generated — everything below is written by `pnpm enrich`; do not edit by hand -->
