# Verification report — 004-classifier-harness

2026-09-12T12:36:11.013Z · rejected · 42s · 77,634 ET

## Summary

The one requirement accs.md marks with a shouted warning - guaranteeing only the mock is ever reached - is implemented as a best-effort heuristic that silently no-ops when the mock isn't reachable, so a run against a live GitHub instance could still pass the check.

## Scope of the fix

Redo the ACCS only.

## Why rejected

- **Coverage** "unset GITHUB_TOKEN GH_TOKEN GITHUB_ACCESS_TOKEN 2>/dev/null || true" — This only removes a few guessed env var names as a deterrent; it never asserts the harness was actually pointed at the mock (no check of a mock base-URL config, no failure if the mock is down and the harness talked to something else). The one line in accs.md written in caps with an exclamation mark is left unenforced.
- **Coverage** "else
  echo "NOTE: mock GitHub not reachable at $MOCK_GITHUB_URL, skipped comment-count guard" >&2
fi" — When the mock isn't reachable, the 'nothing was posted anywhere' guard is skipped entirely instead of failing, so a broken or missing mock silently passes the exact check accs.md requires the ACCS to perform.

## Should fix

- **Method** "COMMENTS_BEFORE_INJ=$(curl -sS "$MOCK_GITHUB_URL/issues/$INJECTION_ID/comments" 2>/dev/null | jq 'length' 2>/dev/null || echo "unknown")" — If curl or jq fails mid-way this degrades to "unknown" and the before/after comparison is quietly skipped for that fixture, further softening the no-comment guarantee without surfacing it as a failure.

## Worth to consider

(none)
