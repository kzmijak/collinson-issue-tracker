# ACCS fix report — 004-classifier-harness

2026-09-12T12:36:52.833Z · ACCS rewritten · 49s · 94,531 ET

## What it was fixed after

The one requirement accs.md marks with a shouted warning - guaranteeing only the mock is ever reached - is implemented as a best-effort heuristic that silently no-ops when the mock isn't reachable, so a run against a live GitHub instance could still pass the check.

## ACCS — general flow

1. Require jq and the fixtures file to exist
2. Hash the repo's own .generated dir before anything runs
3. Strip GitHub creds as a deterrent, then require the mock to actually answer a request - if it doesn't, fail immediately instead of degrading the check
4. Pick one injection-tagged fixture and one ordinary fixture from fixtures/harness-issues.json
5. Read the before-run comment counts on both fixtures straight from the mock, failing hard if curl or jq can't confirm a real number
6. Run pnpm harness restricted to one config and those two fixtures, capturing the runId
7. Load the machine-readable output, confirm exactly 2 entries, confirm expected priority/effort match the fixtures file, confirm every required field is present
8. Hand-check the penalty formula on one entry
9. Load the readable table, confirm exactly one row for the config, confirm all seven metric columns are present, non-blank, and in range
10. Re-read comment counts on both fixtures from the mock and require them to be byte-identical to the before snapshot, failing hard on any read problem
11. Re-hash the repo's .generated dir and require it to be unchanged
12. Clean up the temp GENERATED_DIR on exit no matter how the script ends
