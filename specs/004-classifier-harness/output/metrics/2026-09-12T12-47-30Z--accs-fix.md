# ACCS fix report — 004-classifier-harness

2026-09-12T12:46:40.703Z · ACCS rewritten · 50s · 87,255 ET

## What it was fixed after

The previous path bug is fixed, but accs.bash only checks the combined-accuracy column against accs.md's numeric rules and silently skips the rest — priority accuracy, effort accuracy, total ET, total time, and failures-as-integer are never validated.

## ACCS — general flow

1. Locate the injection fixture and one ordinary fixture, and the first configured config's name
2. Snapshot the repo's own .generated dir and spec 003's classifications.jsonl (under the temp GENERATED_DIR/003-issue-classifier layout) before running anything
3. Point GENERATED_DIR at a fresh temp dir, run the harness restricted to that one config and those two fixtures, capture the printed runId
4. Assert both output files exist, have exactly 2 entries/1 row, carry all required fields, and match the fixture's expected priority/effort
5. Recompute the penalty formula by hand for one entry and compare
6. Range/type-check every metric column in the md row: combined, priority and effort accuracy in 0-1, failures as a whole number, totalEt and totalTimeMs both above zero, and no blank cells
7. Call pnpm classify --spec-id 003-issue-classifier --issue-id <ordinary fixture> --harness and check it prints exactly one JSON line with the expected fields
8. Confirm classifications.jsonl and the repo's own .generated dir are byte-identical to their before-snapshots
9. Clean up the temp dir on exit either way, then report PASS
