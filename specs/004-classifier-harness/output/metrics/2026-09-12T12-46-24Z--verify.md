# Verification report — 004-classifier-harness

2026-09-12T12:45:36.354Z · rejected · 48s · 72,760 ET

## Summary

The previous path bug is fixed, but accs.bash only checks the combined-accuracy column against accs.md's numeric rules and silently skips the rest — priority accuracy, effort accuracy, total ET, total time, and failures-as-integer are never validated.

## Scope of the fix

Redo the ACCS only.

## Why rejected

- **Coverage** "Every accuracy is a number between 0 and 1. Total ET and total time are above zero. Failures is a whole number." — accs.bash only range-checks the combined accuracy column ($3) and never touches priority accuracy, effort accuracy, totalEt, totalTimeMs or failures. A broken implementation that reports totalEt=0, a negative time, or a non-integer failures count would still pass.

## Should fix

- **Method** "CLASSIFICATIONS_FILE="$TMP_GEN/$SPEC_ID/../003-issue-classifier/classifications.jsonl"" — This assumes spec 003 writes to $GENERATED_DIR/003-issue-classifier/classifications.jsonl, a path convention never stated anywhere in this spec or its contract. If the real layout differs the before/after hashes both stay empty and the check silently passes without ever comparing anything.
- **Method** "CLASSIFY_OUT=$(pnpm classify --spec-id "$SPEC_ID" --issue-id "$ISSUE_FOR_CLASSIFY" --harness 2>&1)" — Passing 004-classifier-harness as the classify command's --spec-id is unexplained by the contract; if --spec-id is meant to route to the issue's owning spec this call may target the wrong context.

## Worth to consider

- **Coverage** "The harness talks to no GitHub service at all, real or mock." — Nothing in accs.bash actively verifies absence of a GitHub connection (no mock service is even started); this rests entirely on the implementation's own restraint rather than being independently checked.
