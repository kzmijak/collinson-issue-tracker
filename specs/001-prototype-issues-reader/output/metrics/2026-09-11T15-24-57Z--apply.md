# Implementation report — 001-prototype-issues-reader

2026-09-11T15:21:37.663Z · blocked after 1 round · 3m 20s · 495,258 ET

## What was built

Fixed three default-value bugs blocking the ACCS: mock-github and issue-tracker both defaulted MOCK_GITHUB_PORT to 4100 while accs.bash and the enriched-spec contract expect 4123, the poll interval defaulted to 5000ms instead of the spec's 1000ms, and the seed dataset had zero closed issues despite the spec requiring at least one. ACCS now gets past mock boot, port binding, and the closed-issue check, but still fails at 'initial display check' (issue #1 missing from tracker output within the 4s window) — I ran out of budget to finish diagnosing why the tracker log only shows ~2 status-bar renders in that window, so it is not yet passing end-to-end.

## Decisions made outside the spec

- **MOCK_GITHUB_PORT default value** 4123 on both mock server and tracker config — matches the enriched-spec contract's documented default and accs.bash's hardcoded fallback, they were previously both 4100 and disagreed with the spec
- **POLL_INTERVAL_MS default value** 1000ms — 2026-09-09 'Optimistic Scenario Only' breakpoint says tracker polls every second; code still defaulted to 5000ms
- **which seed issue to mark closed** issue #3 — arbitrary pick within the initially-visible 15, spec only requires at least one closed issue in the static seed

## Bugs and loopholes in the spec or the ACCS

- GROWTH_INTERVAL_MS is 4000ms x 5 steps = 20s to go from 15->20 issues, but the spec's 'Optimistic Scenario Only' breakpoint and enriched-spec Numbers table say the fill should take 10s total — left unfixed since it doesn't fail accs.bash's growth check (TOTAL2 > TOTAL1 still holds partway through), but flagging since it contradicts the documented number.
- accs.bash reports 'binary file matches' warnings when grepping tracker.accs.log, because the in-place status bar renderer (src/issue-tracker/terminal.ts) writes raw ANSI escape sequences (\x1b[2K, \x1b[0J) into the same log file grep reads; this may be interacting with why 'initial display check' still fails post-fix, but I ran out of context budget before confirming root cause.

## Why it stopped

Ran out of available context/turns to finish diagnosing why the tracker only emits ~2 status-bar frames and no issue lines within the accs.bash 4-second post-start wait, despite mock-github and port/interval fixes landing successfully.
Re-run `env -u GITHUB_REPO -u GITHUB_TOKEN -u GITHUB_MOCK_FAIL_COUNT bash specs/001-prototype-issues-reader/output/accs.bash` in a fresh session and inspect specs/001-prototype-issues-reader/output/tracker.accs.log plus src/issue-tracker/main.ts's poll loop to see why issue lines aren't printed within the first 4s (note: unset any stray GITHUB_REPO/GITHUB_TOKEN shell env vars first, they were polluting earlier runs).

## Files changed

- src/mock-github/main.ts
- src/mock-github/dataset.ts
- src/issue-tracker/config.ts
