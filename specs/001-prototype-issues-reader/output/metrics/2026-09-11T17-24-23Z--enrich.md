# Enrichment report — 001-prototype-issues-reader

2026-09-11T17:22:22.566Z · written · 2m 1s · 192,246 ET

## Assumptions made

(none)

## Expected drawbacks

- This fix only guards against the redraw-fragment mismatch; it still can't prove the tracker never rewrites an already-committed line — that remains bounded by the whole-word issue-number + line-count checks already in place, same as before.

## Contract

| facade | promise |
| ------ | ------- |
| bash specs/001-prototype-issues-reader/output/accs.bash | single entry point, exits 0 when the world matches this spec, exits 1 otherwise, wrapped end-to-end in `timeout 60` |
| GET /repos/{owner}/{repo}/issues | mock GitHub API's issues endpoint, mirrors real GitHub REST shape including per-issue `state` of 'open' or 'closed' |
| GITHUB_REPO env var | holds the combined 'owner/repo' string; accs.bash must build the URL as /repos/${GITHUB_REPO}/issues |
| pnpm mock-github | starts the HTTP server, seeded with 15 issues at boot (at least one closed), growing to 20 within 10s measured from the moment the server first answers a request |
| pnpm prototype-issues-reader | console process printing every open issue's number as a whole word outside the Polling line, appending only, never printing a closed issue's number, with a live Polling status line with 1-3 cycling dots, written in-place via carriage-return redraw and never newline-terminated |
| every curl call inside accs.bash | invoked with --max-time 5 |
| background process launch inside accs.bash | mock-github and prototype-issues-reader are each started via `setsid <cmd> ... &`, so their PID is a killable group leader |
| accs.bash log files (mock.accs.log / tracker.accs.log) | each run writes to a fresh, uniquely-named pair of log files, never truncating a shared filename |
| append-only snapshot method inside accs.bash | both the early snapshot and the later recheck extract the tracker log via `head -n "$SNAP_LINES" "$TRACKER_LOG"` — never `cp` or any raw byte-copy of the live file — so an in-flight, non-newline-terminated status-bar fragment can never be captured as if it were a complete line; the two `head -n` outputs, taken at different times against the same $SNAP_LINES, must be byte-identical |

## ACCS — general flow

1. Re-exec self under `timeout 60` on first run so the whole check is hard-bounded regardless of caller
2. cd to repo root, launch `pnpm mock-github` via setsid into a uniquely-named log, capturing its PID as the killable group leader
3. Probe the issues endpoint up to 3 times (1.4s gaps, 5s curl timeout) until it answers; record that moment as T0 and the initial total/closed counts, failing if no closed issue exists in the seed
4. Launch `pnpm prototype-issues-reader` via setsid into a uniquely-named log, with GITHUB_REPO exported
5. After 2s, take the first append-only snapshot: SNAP_LINES = wc -l, SNAPSHOT_1 = head -n SNAP_LINES
6. Sleep until 4s since T0, re-curl, and assert the total issue count grew past the initial count
7. Sleep a bit more for the reader's next poll, assert the tracker log grew past SNAP_LINES on its own (no manual trigger), then re-extract head -n SNAP_LINES and assert it's byte-identical to SNAPSHOT_1
8. Sleep until 13s since T0 (10s growth + 3s settle), re-curl, and assert the dataset settled at exactly 20 issues
9. Pull open vs closed issue numbers from that final response; assert every open number appears in the tracker log and no closed number ever does
10. Re-run the same head -n SNAP_LINES extraction one more time and assert it still matches SNAPSHOT_1 (proves the earlier lines never mutated)
11. Check the last byte of the tracker log isn't a newline, confirming the Polling bar is still an in-place, unterminated fragment
12. Print PASS and exit 0, or fail() with a specific reason and exit 1; the EXIT trap always kills both process groups so nothing orphans
