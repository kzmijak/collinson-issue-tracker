# Implementation report — 005-triage-fields

2026-09-12T13:44:30.506Z · blocked after 1 round · 10m 56s · 745,730 ET

## What was built

Implemented kind and needsHuman as two extra fields on the single classifier LLM call, threaded through Classification/jsonl, and appended as two extra comment-table columns; all existing tests plus spec 003's own accs.bash still pass unchanged. Spec 005's own accs.bash cannot succeed as written: it assumes classifications.jsonl is still discoverable after invoking spec 003's accs.bash, but that script writes the file into a mktemp GENERATED_DIR that its own `trap cleanup EXIT` deletes before control returns.

## Decisions made outside the spec

- **how to handle an invalid/missing kind from the model** default to 'noise', reusing the same silent-clamp pattern clampPriority/clampEffort already use — spec says invalid kind is handled by whatever repair path 003 already uses for priority/effort, and that path is a silent default, not a retry loop
- **where to put the priority==5 forces needsHuman=true guard** co-located it in llmClassifier.ts next to clampPriority/clampEffort as clampNeedsHuman, applied before ClassifyResult is returned — keeps every response-shaping rule for the one LLM call in a single file, and still runs before the entry reaches the store or a posted comment

## Sent back

Redo the ACCS only.

## Bugs and loopholes in the spec or the ACCS

- **Coverage** "echo "[005] spec 003 check passed. That run already did a full classifier pass, so jsonl and comments are fresh."" — This assumes classifications.jsonl survives after spec 003's accs.bash returns, but that script exports GENERATED_DIR=$TMP_ROOT/.generated and its own `trap cleanup EXIT` runs `rm -rf "$TMP_ROOT"` on exit, deleting the jsonl before 005's script ever searches for it. Running the full accs.bash reproduces this: spec 003's section prints ACCS PASS, then 005's own jsonl-lookup immediately fails with 'could not locate classifications.jsonl'.
- **Method** 005's accs.bash has no way to point spec 003's run at a durable path — it never sets GENERATED_DIR itself before shelling out to specs/003-issues-classifier/output/accs.bash, so it has no path to inspect afterward no matter how the classifier code behaves.

## Why it stopped

specs/005-triage-fields/output/accs.bash cannot pass as written: it depends on classifications.jsonl existing after invoking specs/003-issues-classifier/output/accs.bash, but that script's own EXIT trap deletes the temp directory holding that file before returning.
Rewrite specs/005-triage-fields/output/accs.bash to export GENERATED_DIR (and CLASSIFICATIONS_JSONL_PATH) to a path it controls before invoking spec 003's accs.bash, so the jsonl survives that script's cleanup trap for 005's own checks to read.

## Files changed

- src/issue-tracker/classificationTypes.ts
- src/issue-tracker/llmClassifier.ts
- src/issue-tracker/classifier.ts
- src/issue-tracker/classificationComment.ts
- tests/issue-tracker/classifier.test.ts
- tests/issue-tracker/classificationComment.test.ts
- tests/issue-tracker/classificationsStore.test.ts
