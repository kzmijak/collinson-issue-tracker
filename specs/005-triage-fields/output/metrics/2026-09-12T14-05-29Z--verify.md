# Verification report — 005-triage-fields

2026-09-12T14:05:03.791Z · approved · 26s · 44,220 ET

## Summary

The prior false-pass hole is fixed - missing issue numbers now hard-fail the run instead of being silently skipped. No new blocking issues found.

## Worth to consider

- **Method** "if ! echo "$BODY" | grep -qi "$ENTRY_KIND"; then" — grep -qi matches ENTRY_KIND/ENTRY_NH as a bare substring anywhere in the comment body, not scoped to the Kind/Needs Human columns specifically. A comment that happens to contain the right word elsewhere (or a wrong table with the right word in prose) would still pass.
- **Coverage** "should noise issues still get the full table (priority/effort/kind/needsHuman) or a short-circuited one? Recommendation: yes, full table" — This is a reasonable default but remains an open question rather than a confirmed decision, worth folding into a breakpoint if the operator disagrees.
