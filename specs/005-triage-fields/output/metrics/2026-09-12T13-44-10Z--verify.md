# Verification report — 005-triage-fields

2026-09-12T13:43:24.795Z · approved · 46s · 73,221 ET

## Summary

The enriched spec stays faithful to the man-made section and the ACCS actually checks what accs.md asks for, including the priority-5 forcing rule and the comment cross-check. A couple of jq/grep fragilities are worth tightening but they don't let a wrong implementation pass.

## Worth to consider

- **Coverage** "ISSUE_NUMBER=$(echo "$line" | jq -r '.issueNumber // .issue_number // empty')" — The field name used to look up the issue number for the comment cross-check is guessed (two candidate keys) since neither spec.md, accs.md, nor the contract names spec 003's jsonl issue-number key. Fine as a pragmatic guess, but if spec 003 actually uses a third name, this check silently no-ops via the `[ -z "$ISSUE_NUMBER" ] && continue` skip.
- **Scope** "should noise issues still get the full table (priority/effort/kind/needsHuman) or a short-circuited one? Recommendation: yes, full table" — This is a reasonable default but it's an assumption made without the operator, not something spec.md states explicitly — worth confirming in the next breakpoint if noise handling ever needs to diverge.
