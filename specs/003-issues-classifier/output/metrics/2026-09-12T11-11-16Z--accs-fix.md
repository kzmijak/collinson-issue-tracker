# ACCS fix report — 003-issues-classifier

2026-09-12T11:10:43.119Z · ACCS rewritten · 33s · 67,555 ET

## What it was fixed after

The two previous findings are fixed (full jsonl shape/range validation and marker+prose+table checks are now in place), but accs.bash has a fresh bug: a stray trailing space in the REPO_ROOT path breaks the cd and makes the whole script exit before running any check.

## ACCS — general flow

1. Resolve repo root by trimming the stray trailing space from the '../../..' path so cd succeeds and the script actually runs
2. Load .env, spin up the mock GitHub in a temp log, poll until it answers 200/304
3. List all issue ids from the mock
4. Run 'pnpm classify --spec-id 003-issues-classifier' pointed at a fresh temp GENERATED_DIR, capture stdout
5. Assert exit 0, no REPOSTED lines yet, jsonl file exists with a valid full-shape entry per issue, and a marker+prose+table comment exists on GitHub per issue
6. Kill and restart the mock to simulate GitHub memory loss, confirm markers are gone
7. Re-run classify against the same temp GENERATED_DIR, assert exit 0, no CLASSIFIED lines (no LLM calls), at least one REPOSTED line
8. Assert jsonl line count is unchanged (upsert, not rewrite) and every issue again has a valid entry and a valid marker comment on GitHub
9. Stop the mock and clean up the temp GENERATED_DIR via trap, fail loudly if any check above failed
