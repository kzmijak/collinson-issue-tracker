# ACCS fix report — 003-issues-classifier

2026-09-12T11:09:30.818Z · ACCS rewritten · 48s · 88,100 ET

## What it was fixed after

Enriched-spec.md is faithful to spec.md/accs.md, but accs.bash under-checks the jsonl contract — it only confirms an issueId exists, not the full required shape.

## ACCS — general flow

1. Boot the mock GitHub, run `pnpm classify` once, and confirm it exits 0 with no premature REPOSTED lines
2. For every discovered issue, validate the jsonl entry has all five required fields with priority/effortEst inside their spec'd ranges, not just a matching issueId
3. For every discovered issue, confirm the GitHub comment's first line is the exact marker and that the rest of the body has real prose plus a markdown table, not just the marker alone
4. Restart the mock to simulate GitHub memory loss, confirm markers are actually gone, then re-run classify and expect REPOSTED (no LLM calls) with an unchanged jsonl line count
5. Re-validate full jsonl shape and marker+prose+table on GitHub after the replay pass
6. Fail loudly and exit 1 on any gap; clean up the mock process and the temp GENERATED_DIR on exit either way
