# ACCS fix report — 002-early-alpha-issues-reader

2026-09-12T10:37:49.062Z · ACCS rewritten · 43s · 77,963 ET

## What it was fixed after

Both must-fix items from the last verdict are properly fixed, but the script never checks the Mock GitHub terminal's own comment-count display, only the GET /issues API — so the one feature accs.md explicitly asks to watch (the terminal logging a comment count) goes untested.

## ACCS — general flow

1. Start mock-github, confirm issues load with zero comments, confirm linear growth after 4s, sweep the log for duplicate/cap violations across snapshots
2. POST a comment on the first issue, confirm the one-shot '[comment] +1' log line appears
3. Wait one repaint cycle, then pull just the last snapshot block (single awk pass, no scratch files) and grep it for 'Issues #<first>: (1)' — this is the new check that closes the rejection: the terminal's own rendered count must reflect the mutation, not just the API or the log line
4. Confirm via GET /issues that the comment body shows up in memory
5. Run issues-tracker for one pass, confirm via GET /issues that the first issue ends at 2 comments (original + tracker) and every other issue has 0 or 1, all tracker-authored ones saying 'I've been here!'
6. Wait one more repaint cycle, pull the final last-snapshot block, and grep it for 'Issues #<first>: (2)' plus, for every other issue id, its exact matching count from GET /issues — this proves the terminal's comment-count display actually stays in sync post-tracker, not just the API
7. Re-run the same single-pass duplicate/cap sweep over the whole log to make sure nothing repeated within any screen
8. Kill everything by port (never by matching a 'pnpm mock-github' string) during cleanup
