# Implementation report — 002-early-alpha-issues-reader

2026-09-12T09:42:42.248Z · blocked after 1 round · 5m 23s · 926,198 ET

## What was built

Finished bringing mock-github and the issues tracker in line with spec 002's latest enriched contract (flat /issues shape with id/content, snapshot-marker terminal repaint, buffer-cap truncation, updated port/interval defaults) and fixed two pre-existing test bugs so the full suite (220 tests) and tsc are clean. I could not get output/accs.bash to exit 0: step 1 unconditionally fails because the script exports MOCK_GITHUB_PORT=4000 before invoking spec 001's frozen accs.bash, whose own probe uses an unrelated 4123 default — I verified by hand that every other step (2 through 8) passes once that ordering bug is bypassed.

## Decisions made outside the spec

(none)

## Sent back

Redo the ACCS only.

## Bugs and loopholes in the spec or the ACCS

- **Method** "export MOCK_GITHUB_PORT="$MOCK_PORT"" — This runs before step 1 invokes spec 001's own frozen accs.bash, so the mock-github process that script spawns inherits MOCK_GITHUB_PORT=4000 while that script's own curl probe still targets its independently-computed default of 4123 (it has no knowledge of MOCK_GITHUB_PORT). The two scripts can never agree on a port in this order, so the smoke test fails every single time regardless of what src/ contains, blocking every later check in the file.

## Why it stopped

specs/002's accs.bash exports MOCK_GITHUB_PORT=4000 globally before running spec 001's accs.bash, which probes its own hardcoded default port 4123 — the ports can never converge in this call order, so the ACCS cannot reach exit 0 no matter what the implementation does.
Edit specs/002-early-alpha-issues-reader/output/accs.bash to invoke the spec-001 smoke test before exporting MOCK_GITHUB_PORT/MOCK_GITHUB_URL (e.g. run it as `env -u MOCK_GITHUB_PORT -u MOCK_GITHUB_URL bash "$SPEC001_ACCS"`), then re-run `bash specs/002-early-alpha-issues-reader/output/accs.bash`.

## Files changed

- .env.example
- src/issue-tracker/mockGithubClient.ts
- src/issue-tracker/proactiveConfig.ts
- src/issue-tracker/proactivePoller.ts
- src/mock-github/main.ts
- src/mock-github/server.ts
- src/mock-github/terminal.ts
- tests/issue-tracker/proactiveConfig.test.ts
- tests/issue-tracker/proactivePoller.test.ts
- tests/mock-github/terminal.test.ts
