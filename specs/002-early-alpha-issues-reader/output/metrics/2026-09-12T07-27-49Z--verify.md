# Verification report — 002-early-alpha-issues-reader

2026-09-12T07:26:38.429Z · rejected · 1m 11s · 112,075 ET

## Summary

The spec-001 lookup glob and the ACCS's own file path contradict the repo convention and each other, so the script will likely fail before testing anything real.

## Scope of the fix

Redo the enriched spec and the ACCS.

## Why rejected

- **Method** "SPEC001=$(ls specs/001-*/output/accs.bash 2>/dev/null | head -n1)" — The dictionary's repo layout puts accs.bash at the spec root (sibling to spec.md), not under output/. If spec 001 follows that convention, this glob finds nothing and the whole script aborts at step 1 regardless of implementation quality.
- **Method** "file: specs/002-early-alpha-issues-reader/output/accs.bash" — This contradicts the 'Done when' line ('specs/002-early-alpha-issues-reader/accs.bash exits 0') and the dictionary's stated layout, which places accs.bash at the spec root, not inside output/. Two different locations are asserted for the same script inside the same document.
- **Coverage** "MAKE SURE THAT ONLY THE MOCK INSTANCES ARE BEING USED!" — The enriched spec elevates this into a full 'mock-only enforcement' contract clause (tracker exits non-zero on bad GITHUB_MODE, before any network call) and a dedicated Behaviour row, but accs.bash never exercises that path — it only ever exports GITHUB_MODE=mock and never runs the tracker with it unset/invalid. The enforcement guarantee ships untested.

## Should fix

- **Coverage** "stdout line: '--- REPAINT ---' | Printed as the first line of every terminal repaint" — The whole terminal-repaint contract (marker line, per-issue block shape, 'New comment on issue #<n>' log) has no assertion anywhere in accs.bash — it checks only the HTTP JSON side. Given how much of spec.md's original ask is about what the terminal prints, this leaves that half of the feature unverified by the idempotency guard.

## Worth to consider

- **Scope** "First issue form the GitHub terminal should have 2 comments, the rest should have 1, it is acceptable that of the later entries have 0." — The enricher chose to verify counts via the HTTP API instead of parsing terminal stdout — a reasonable reading of the man-made accs.md wording, but worth the operator knowing this was a judgment call, not a literal terminal-log check.
