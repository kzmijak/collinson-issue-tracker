# ACCS fix report — 002-early-alpha-issues-reader

2026-09-12T09:29:08.944Z · ACCS rewritten · 59s · 155,502 ET

## What it was fixed after

The prior buffer-cap blocker is fixed via MOCK_GITHUB_BUFFER_CAP, but the ACCS never checks the terminal actually prints the comment-added log line, which accs.md explicitly asks for.

## ACCS — general flow

1. Fix port default mismatch: MOCK_PORT now falls back to 4000, matching the contract's documented default.
2. Add assert_comment_logged helper: polls mock-github.log for up to 2s for the exact '[comment] issue #<id> +1 from <author>' line.
3. Call assert_comment_logged right after the manual QA curl POST in step 5, before checking the comment count — this is the missing coverage the reviewer flagged.
4. Also call assert_comment_logged for the tracker's own touch on the first issue in step 7, so both manual and automated comment paths are proven to hit the log line, not just the state mutation.
5. Everything else (spec 001 smoke test, snapshot dedupe scanning, growth check, tracker sweep, final integrity checks) is unchanged from the previous accs.bash.
