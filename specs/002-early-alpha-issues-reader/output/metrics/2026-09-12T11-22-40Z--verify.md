# Verification report — 002-early-alpha-issues-reader

2026-09-12T11:22:10.775Z · approved · 30s · 64,561 ET

## Summary

The blocking awk bug is fixed: check_snapshots() now has a BEGIN block setting lastN=-1, so the first marker (n=0) no longer false-fails as nonmonotonic. The stale port-4000 assertion is also gone, PORT now defaults to 4123 matching the contract.

## Worth to consider

- **Coverage** "BEGIN { lastN = -1 }" — This single init line was the entire root cause from the last rejection and is now confirmed present; no other logic in check_snapshots() changed, so the rest of the previously-working dup/cap/truncation checks are unaffected.
